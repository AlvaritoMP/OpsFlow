[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npm run build",
  "node scripts/protect-dist.js"
]

# Asegurar que el index.html compilado no se sobrescriba
# Copiar solo los archivos necesarios después del build
[phases.start]
cmd = "exec caddy run --config /assets/Caddyfile --adapter caddyfile 2>&1"
